spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/investment_helper_analytics
    username: postgres
    password: your_local_password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

analytics:
  risk-free-rate: 0.05